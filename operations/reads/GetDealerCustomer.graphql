query GetDealerCustomer($dealerAccountId: ID!, $customerAccountId: ID!) {
  dealerCustomerById(
    dealerAccountId: $dealerAccountId
    customerId: $customerAccountId
  ) {
    id
    companyName
    addresses {
      id
      address1
      city
    }
    defaultShipToContact {
      id
      firstName
      lastName
      email
    }
    defaultShipToAddress {
      id
      address1
      city
    }
    contacts {
      id
      firstName
      lastName
      email
    }
  }
}
